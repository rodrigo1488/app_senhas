# Core Flask e WebSocket
Flask==3.1.0
Flask-SocketIO==5.5.1
Flask-Cors==5.0.0
Werkzeug==3.1.3

# ORM (mapeia o schema existente do appsenhas.sqlite — ver app/models.py)
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36

# Driver PostgreSQL (banco padrão em produção/Docker — ver DATABASE_URL em
# backend/__init__.py e docker-compose.yml). SQLite continua funcionando
# como fallback de dev sem Docker.
psycopg2-binary==2.9.13

# Token de sessão por código de setor (web kiosk + app Android + sockets)
PyJWT==2.10.1

# Eventlet para SocketIO
eventlet==0.39.0
dnspython==2.7.0

# Impressão térmica
python-escpos==3.1

# Variáveis de ambiente
python-dotenv>=1.0.0

# Fuso America/Sao_Paulo via zoneinfo (fallback se o SO não tiver tzdata)
tzdata>=2025.1

# QR Code
qrcode==8.0

# Processamento de imagens
Pillow==11.0.0

# Notificações Push
pywebpush==1.14.1
cryptography>=42.0.5

# Servidor WSGI para produção (opcional)
gunicorn==23.0.0
